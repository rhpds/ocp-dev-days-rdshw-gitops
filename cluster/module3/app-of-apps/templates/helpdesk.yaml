{{- if .Values.helpdesk.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
{{ include "app-of-apps.metadata" (dict "name" .Values.helpdesk.name "namespace" .Values.argocd.namespace "syncWave" "4") }}
spec:
  destination:
    namespace: {{ .Values.helpdesk.namespace }}
    server: https://kubernetes.default.svc
  project: default
  source:
    path: cluster/module3/helpdesk
    repoURL: {{ .Values.gitops.repoURL }}
    targetRevision: {{ .Values.gitops.targetRevision }}
    helm:
      valuesObject:
        namespace: {{ .Values.helpdesk.namespace }}
        cluster:
          subdomain: {{ .Values.cluster.subdomain }}
{{ include "app-of-apps.syncPolicy" . | indent 2 }}
{{- end }}
