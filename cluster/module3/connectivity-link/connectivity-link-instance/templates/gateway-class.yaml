apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gatewayClass.name }}
  labels:
    {{- include "connectivity-link.labels" . | nindent 4 }}
  annotations:
    {{- include "connectivity-link.gatewayclass-syncwave" . | nindent 4 }}
spec:
  controllerName: {{ .Values.gatewayClass.controllerName }}
